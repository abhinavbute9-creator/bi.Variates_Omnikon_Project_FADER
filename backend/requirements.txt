# FADER backend — dependencies
# Install with: pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
alembic==1.13.3
httpx==0.27.2
python-dotenv==1.0.1

# Only needed if you switch DATABASE_URL to Postgres instead of the default SQLite.
# psycopg2-binary==2.9.9
